# ATS Resume Checker using NLP & Machine Learning

An AI-powered Applicant Tracking System (ATS) Resume Checker that analyzes resumes against job descriptions using advanced NLP techniques (BERT / Sentence Transformers) and ranks candidates based on semantic similarity.

## 🚀 Features
- Resume parsing (PDF / Text)
- Job Description vs Resume semantic matching
- BERT / Sentence Transformer embeddings
- Cosine similarity–based ranking
- ATS score generation
- Streamlit web interface

## 🧠 Tech Stack
- Python
- Streamlit
- PyTorch
- Hugging Face Transformers
- Sentence Transformers
- Scikit-learn
- Pandas, NumPy

## 📂 Project Structure

## ⚙️ Installation
```bash
git clone git@github.com:KabilashKS/ATS-Resume-checker.git
cd ATS-Resume-checker
pip install -r requirements.txt


4️⃣ Save:
- `CTRL + O` → Enter
- `CTRL + X`

---

## ❗ Important
- ❌ **Do NOT run `git clone` inside your own repo**
- ❌ **Do NOT paste markdown into terminal**
- ✅ This is for **users / recruiters** reading GitHub

---

## 🧠 When DO you use terminal?
Only for **commands**, like:
```bash
pip install -r requirements.txt
streamlit run app.py
